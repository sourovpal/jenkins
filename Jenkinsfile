pipeline {
  agent any
  stages {
    stage('Build') {
      steps {
        echo "Echo: Build project"
      }
    }
    stage('Deploy') {
      steps {
        echo "Echo: Deploy project"
      }
    }
  }
}
